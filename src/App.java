import service.MenuHandler;

public class App {

    // Main method to start the application
    public static void main(String[] args) {
        // Call the main menu method from MenuHandler to show the menu options
        MenuHandler.mainMenu();
    }
}
